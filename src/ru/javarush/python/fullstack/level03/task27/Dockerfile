# Первый этап: сборка React-приложения
FROM node:14 AS build

# Установка рабочего каталога
WORKDIR /app

# Копирование package.json и package-lock.json для установки зависимостей
COPY package.json package-lock.json ./

# Установка зависимостей
RUN npm ci --only=production
# ci  команда используется для установки зависимостей проекта из package-lock.json или npm-shrinkwrap.json.
# Она гарантирует, что зависимости устанавливаются точно в тех версиях, которые указаны в этих файлах,
# что способствует предсказуемости и воспроизводимости сборки. npm ci быстрее, чем npm install,
# так как не обновляет package-lock.json и удаляет существующие модули перед новой установкой.

# --only=production: Этот флаг указывает делать установку только зависимостей, необходимых для выполнения приложения,
# исключая все пакеты, помеченные как devDependencies. Это полезно для производства, так как уменьшает
# размер конечного образа, избавляя от лишних пакетов, которые не нужны для работы приложения, например,
# инструменты для тестирования или разработки.

# Копирование всего исходного кода проекта
COPY . .

# Сборка приложения
RUN npm run build

# Второй этап: развертывание с использованием nginx
FROM nginx:alpine

# Копирование собранного приложения из первого этапа
COPY --from=builder /app/build /usr/share/nginx/html

# Порты, которые будут использоваться для работы приложения
EXPOSE 80

# Запуск Nginx
CMD ["nginx", "-g", "daemon off;"]